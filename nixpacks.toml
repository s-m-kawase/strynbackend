[phases.setup]
nixPkgs = ["python312Full", "postgresql", "libffi", "pkg-config", "gcc"]

[phases.build]
cmds = [
  "python manage.py collectstatic --noinput"
]

[start]
cmd = "gunicorn stryn.wsgi:application --bind 0.0.0.0:${PORT} --workers=3 --timeout=180 --log-file -"
